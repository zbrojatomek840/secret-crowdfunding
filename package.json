{
  "name": "secret-crowdfunding",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter nextjs dev",
    "build": "pnpm --filter nextjs build",
    "hardhat:compile": "pnpm --filter hardhat compile",
    "hardhat:deploy": "pnpm --filter hardhat deploy"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}

